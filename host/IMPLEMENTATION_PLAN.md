# Phase 2 ???? � Tasify Native Messaging Host

## 1. ????

```
host/
+-- package.json
+-- IMPLEMENTATION_PLAN.md
+-- manifest.json                    # Native Messaging Manifest (??)
+-- src/
�   +-- index.js                     # ??:??? Express + Stdio Bridge
�   +-- config.js                    # ????????????
�   +-- http-listener.js             # Express ???,POST /hooks ?? Claude Code ??
�   +-- stdio-bridge.js              # Chrome Native Messaging ???? (4??????)
�   +-- shell-executor.js            # ?? child_process spawn ?? claude-code CLI
�   +-- logger.js                    # ???????
�   +-- utils/
�       +-- buffer-helper.js         # 4-byte Uint32 ?????????
+-- scripts/
�   +-- install-host.ps1             # Windows ??????
�   +-- uninstall-host.ps1           # Windows ????
+-- __tests__/
�   +-- stdio-bridge.test.js         # Stdio ???????
�   +-- shell-executor.test.js       # Shell ?????? (mock child_process)
�   +-- http-listener.test.js        # HTTP ? Stdio ??????
+-- test-helpers/
    +-- simulate-chrome.js           # ??????? stdin/stdout ??
    +-- send-test-hook.sh            # curl ??????
```

## 2. ????

### Step 1: Stdio Bridge ?? (????,????????)
- `src/utils/buffer-helper.js` � 4-byte Uint32BE ???????
- `src/stdio-bridge.js` � ?? `process.stdin` ?? + `process.stdout` ??
- ??:??????????????

### Step 2: Shell Executor
- `src/shell-executor.js` � `spawn claude-code <command>`,?? stdout/stderr
- ? StdioBridge ??:????????? Chrome
- ??:mock child_process,???????????

### Step 3: HTTP Listener
- `src/http-listener.js` � Express `POST /hooks`,?? Claude Code ??
- ?? Token(????,??????)
- ??????? StdioBridge.send() ??? Chrome
- ??:supertest ?? POST ?? + ????

### Step 4: ?? + ??
- `src/config.js` � PORT?TOKEN?CLAUDE_CLI_PATH ?????
- `src/index.js` � ??????,?? Express + Stdio,?? stdin ???????

### Step 5: ???? + ??
- `manifest.json` ??
- `scripts/install-host.ps1` � ????? + ?? manifest
- Integration Guide ??

## 3. ??????

### StdioBridge
```js
class StdioBridge {
  constructor()
  onMessage(handler: (msg: object) => void): void
  send(msg: object): void          // 4-byte length + JSON
  start(): void                    // ???? stdin
  stop(): void                     // ?? + process.exit()
}
```

### HttpListener
```js
class HttpListener {
  constructor(config: { port: number, token?: string, bridge: StdioBridge })
  start(): Promise<void>
  stop(): Promise<void>
  // POST /hooks � receives Claude Code hook events
}
```

### ShellExecutor
```js
class ShellExecutor {
  constructor(config: { cliPath: string, bridge: StdioBridge })
  execute(command: string, params: object): Promise<void>
  // spawn claude-code, pipe stdout to bridge.send()
}
```

## 4. ??????

| ?? | ?? |
|---|---|
| stdin ?? (Chrome ??) | ?? `process.stdin.on("end")`,?? `process.exit(0)` |
| ???? | config ??? `PORT`,??????????? |
| ???? | stdin `data` ??????? Buffer,? 4 ???????? JSON ?? |
| ???? | Chrome ??? kill ????? |
| ???? | ????? Admin ?????? |
| claude-code ??? | ShellExecutor ?? `which claude-code`,????? Stdio ?????? |

## 5. ????

1. ? Stdio Bridge: ? `simulate-chrome.js` ??????,????????
2. ? Stdio ? Shell: ??????? `EXEC_COMMAND`,?? claude-code ???
3. ? HTTP ? Stdio: `curl -X POST http://localhost:3000/hooks`,??????? Stdio ??
4. ? ????: ???? Chrome ??,?? Node.js ????
5. ? ????: ? Windows ??? `install-host.ps1`,????????
